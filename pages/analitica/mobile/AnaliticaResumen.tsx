"use client";

/**
 * Página de Resumen de Analítica - Versión Mobile
 * Dashboard resumen de la sección Analítica
 * (Por implementar)
 */

export function AnaliticaResumenMobile() {
  return (
    <div style={{ height: '100%', width: '100%', padding: 'var(--spacing-lg)' }}>
      <h1 style={{ color: 'var(--foreground)', marginBottom: 'var(--spacing-lg)' }}>
        Resumen de Analítica - Mobile
      </h1>
      <p style={{ color: 'var(--foreground-secondary)' }}>
        Dashboard resumen de la sección Analítica. Aquí se mostrará un resumen general con estadísticas, gráficos y acceso rápido a las subsecciones.
      </p>
    </div>
  );
}
