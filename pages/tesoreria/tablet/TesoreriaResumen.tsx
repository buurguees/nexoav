"use client";

/**
 * Página de Resumen de Tesorería - Versión Tablet Portrait
 * Dashboard resumen de la sección Tesorería
 * (Por implementar)
 */

export function TesoreriaResumenTablet() {
  return (
    <div style={{ height: '100%', width: '100%', padding: 'var(--spacing-lg)' }}>
      <h1 style={{ color: 'var(--foreground)', marginBottom: 'var(--spacing-lg)' }}>
        Resumen de Tesorería - Tablet Portrait
      </h1>
      <p style={{ color: 'var(--foreground-secondary)' }}>
        Dashboard resumen de la sección Tesorería. Aquí se mostrará un resumen general con estadísticas, gráficos y acceso rápido a las subsecciones.
      </p>
    </div>
  );
}
