"use client";

/**
 * Página de Impuestos - Versión Tablet Horizontal
 * (Por implementar)
 */

export function ImpuestosTabletHorizontal() {
  return (
    <div style={{ height: '100%', width: '100%', padding: 'var(--spacing-lg)' }}>
      <h1 style={{ color: 'var(--foreground)', marginBottom: 'var(--spacing-lg)' }}>
        Impuestos - Tablet Horizontal
      </h1>
      <p style={{ color: 'var(--foreground-secondary)' }}>
        Esta vista se desarrollará más adelante con contenido específico.
      </p>
    </div>
  );
}
