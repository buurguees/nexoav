"use client";

/**
 * Página de Resumen de Facturación - Versión Mobile
 * Dashboard resumen de la sección Facturación
 * (Por implementar)
 */

export function FacturacionResumenMobile() {
  return (
    <div style={{ height: '100%', width: '100%', padding: 'var(--spacing-lg)' }}>
      <h1 style={{ color: 'var(--foreground)', marginBottom: 'var(--spacing-lg)' }}>
        Resumen de Facturación - Mobile
      </h1>
      <p style={{ color: 'var(--foreground-secondary)' }}>
        Dashboard resumen de la sección Facturación. Aquí se mostrará un resumen general con estadísticas, gráficos y acceso rápido a las subsecciones.
      </p>
    </div>
  );
}
